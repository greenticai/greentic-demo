[package]
name = "github-mcp-demo"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Greentic GitHub MCP Security Dashboard demo: OAuth, repo scanning, and vulnerability reporting via Adaptive Cards."

[lib]
path = "src/lib.rs"

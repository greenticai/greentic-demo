[package]
name = "cloud-deploy-demo"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "Greentic cloud deployment demo bundle with messaging, events, state, and deploy provider extensions."

[lib]
path = "src/lib.rs"
